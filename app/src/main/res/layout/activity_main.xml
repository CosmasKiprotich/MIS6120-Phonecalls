<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- THIS IS THE NEW SECTION FOR YOUR DETAILS -->
    <LinearLayout
        android:id="@+id/labDetailsLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Phone Calls By Cosmas Kiprotich"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="?attr/colorPrimary" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MIS6120 – Mobile Computing"
            android:textSize="16sp" />

    </LinearLayout>

    <!-- This will display the phone number -->
    <EditText
        android:id="@+id/phoneNumberEditText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:autofillHints="phone"
        android:background="@android:color/transparent"
        android:gravity="center"
        android:hint="Enter number"
        android:inputType="phone"
        android:padding="16dp"
        android:textSize="32sp"
        app:layout_constraintEnd_toStartOf="@id/deleteButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/labDetailsLayout"
        app:layout_constraintBottom_toTopOf="@id/dialerGrid"
        app:layout_constraintVertical_bias="0.1"/>

    <!-- Delete Button -->
    <ImageButton
        android:id="@+id/deleteButton"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Delete"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:src="@android:drawable/ic_input_delete"
        app:layout_constraintBottom_toBottomOf="@id/phoneNumberEditText"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/phoneNumberEditText" />

    <!-- This GridLayout holds all the dialer buttons -->
    <GridLayout
        android:id="@+id/dialerGrid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="4"
        app:layout_constraintBottom_toTopOf="@id/callButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/phoneNumberEditText"
        app:layout_constraintVertical_chainStyle="packed">

        <!-- All the number buttons remain the same -->
        <Button android:id="@+id/button1" style="@style/DialerButton" android:onClick="onDialerClick" android:text="1" />
        <Button android:id="@+id/button2" style="@style/DialerButton" android:onClick="onDialerClick" android:text="2" />
        <Button android:id="@+id/button3" style="@style/DialerButton" android:onClick="onDialerClick" android:text="3" />
        <Button android:id="@+id/button4" style="@style/DialerButton" android:onClick="onDialerClick" android:text="4" />
        <Button android:id="@+id/button5" style="@style/DialerButton" android:onClick="onDialerClick" android:text="5" />
        <Button android:id="@+id/button6" style="@style/DialerButton" android:onClick="onDialerClick" android:text="6" />
        <Button android:id="@+id/button7" style="@style/DialerButton" android:onClick="onDialerClick" android:text="7" />
        <Button android:id="@+id/button8" style="@style/DialerButton" android:onClick="onDialerClick" android:text="8" />
        <Button android:id="@+id/button9" style="@style/DialerButton" android:onClick="onDialerClick" android:text="9" />
        <Button android:id="@+id/buttonStar" style="@style/DialerButton" android:onClick="onDialerClick" android:text="*" />
        <Button android:id="@+id/button0" style="@style/DialerButton" android:onClick="onDialerClick" android:text="0" />
        <Button android:id="@+id/buttonHash" style="@style/DialerButton" android:onClick="onDialerClick" android:text="#" />
    </GridLayout>

    <!-- Call Button -->
    <Button
        android:id="@+id/callButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:text="Call"
        android:paddingLeft="64dp"
        android:paddingRight="64dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
